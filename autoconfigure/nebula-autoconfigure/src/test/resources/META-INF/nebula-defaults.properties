# 测试专用: 模拟 Starter 的默认值文件, 供 NebulaStarterDefaultsIntegrationTest 验证 EPP 是否生效
nebula.test.defaults-sentinel=applied
# 与 application.properties 同名, 用于验证用户配置可覆盖默认值(defaults 为最低优先级)
nebula.test.override-check=from-defaults
