# Nebula Framework 默认配置
# 排除 Spring AI 的默认自动配置，使用 Nebula 的自定义配置

spring:
  # 禁用 Spring Cloud Gateway 内置的 gRPC 功能，使用 Nebula Gateway 的 gRPC 桥接
  cloud:
    gateway:
      grpc:
        enabled: false
  
  autoconfigure:
    exclude:
      # 排除 Spring AI OpenAI 相关自动配置
      - org.springframework.ai.autoconfigure.openai.OpenAiAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiChatAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiEmbeddingAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioSpeechAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioTranscriptionAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiImageAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiModerationAutoConfiguration
      # 排除 Spring AI Chroma 向量存储自动配置
      - org.springframework.ai.vectorstore.chroma.autoconfigure.ChromaVectorStoreAutoConfiguration

