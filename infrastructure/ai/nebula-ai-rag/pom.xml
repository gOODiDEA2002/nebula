<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.nebula-projects</groupId>
        <artifactId>nebula-parent</artifactId>
        <version>${revision}</version>
        <relativePath>../../../pom.xml</relativePath>
    </parent>

    <artifactId>nebula-ai-rag</artifactId>
    <name>Nebula AI RAG</name>
    <description>Nebula framework RAG retrieval, fusion, rerank and pipeline</description>

    <dependencies>
        <!-- Nebula AI Core: 只依赖核心抽象, 不绑定任何具体后端 -->
        <dependency>
            <groupId>com.nebula-projects</groupId>
            <artifactId>nebula-ai-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!--
            Nebula Search Core（可选）: 仅 BM25 关键词检索路（SearchServiceRetriever /
            RagSearchDocument）用它。optional 表示不传递给消费方, 不用关键词检索的应用不必引入;
            装配层用 @ConditionalOnClass(SearchService) 做类存在检查。
        -->
        <dependency>
            <groupId>com.nebula-projects</groupId>
            <artifactId>nebula-search-core</artifactId>
            <version>${project.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- Spring Boot Auto Configuration: 仅用于 @ConfigurationProperties, Bean 装配集中在 nebula-autoconfigure -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>

        <!-- Spring Boot Configuration Processor -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- SLF4J Logging -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!--
            Jsoup HTML解析（可选）: 只有 HtmlStructureParser 用它, 且它是模块内唯一 import jsoup 的类。
            optional 表示不传递给消费方, 不解析 HTML 的应用不必引入; 装配层需做类存在检查。
        -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.17.2</version>
            <optional>true</optional>
        </dependency>

        <!-- 测试：索引 sink 契约用桩验证收到的参数与 ID 集 -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
