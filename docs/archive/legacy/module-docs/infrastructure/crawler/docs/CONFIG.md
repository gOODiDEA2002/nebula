# Nebula Crawler - 配置参考

该模块为爬虫模块组入口，配置主要集中在子模块：

- `nebula.crawler`（核心开关）
- `nebula.crawler.http`
- `nebula.crawler.browser`
- `nebula.crawler.proxy`
- `nebula.crawler.captcha`

## 基础开关

```yaml
nebula:
  crawler:
    enabled: true
```
